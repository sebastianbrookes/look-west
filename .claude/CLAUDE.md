Here are the Resend docs (only explore when necessary): https://resend.com/docs/llms-full.txt

When making commits to the remote repo, DO NOT include yourself as a collaborator or mention your co-authorship.

To push changes to backend, run `npm run dev:backend -- --once`

Run `npx convex dev` to push changes to the cloud environment.

`npx vite` to start the dev environment.

Official link: `https://golookwest.org`